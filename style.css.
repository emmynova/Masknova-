/* style.css - shared site styles (dark theme) */
:root{
  --bg:#05060b; --panel:#0b1220; --accent:#00d9ff; --muted:#9fb3c8; --card:#0f1530;
}
*{box-sizing:border-box}
html,body{height:100%; margin:0; font-family:Inter,system-ui,Segoe UI,Roboto,Arial;}
body{background:linear-gradient(180deg,#02030a,#071127 70%); color:#e6f7ff;}
a{color:var(--accent); text-decoration:none}
.container{max-width:1200px; margin:0 auto; padding:18px;}
/* sidebar + main layout */
.app{display:flex; min-height:100vh;}
.sidebar{
  width:260px; background:linear-gradient(180deg,#07102a,#09142f);
  padding:18px; border-right:1px solid rgba(255,255,255,0.03);
}
.logo{width:44px;height:44px;border-radius:10px;background:linear-gradient(135deg,var(--accent),#6c64ff); box-shadow:0 6px 20px rgba(0,0,0,0.6)}
.brand{display:flex; gap:12px; align-items:center; margin-bottom:10px}
.brand h1{font-size:18px;margin:0}
.nav a{display:block;padding:10px;border-radius:8px;color:#cfeeff;margin:6px 0}
.nav a:hover{background:rgba(0,217,255,0.06); border:1px solid rgba(0,217,255,0.06)}
.main{flex:1; padding:18px; display:flex; flex-direction:column;}
.topbar{display:flex; align-items:center; gap:12px; margin-bottom:14px;}
.search{flex:1; display:flex; align-items:center; gap:10px; background:#06122b; padding:10px; border-radius:10px; border:1px solid rgba(255,255,255,0.03)}
.search input{background:transparent;border:none;outline:none;color:#e6f7ff; width:100%}
.grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:14px}
.card{background:linear-gradient(180deg,#071133,#07122a); border:1px solid rgba(255,255,255,0.03);
  padding:16px; border-radius:12px; box-shadow: 0 6px 18px rgba(0,0,0,0.6); cursor:pointer; transition:transform .18s, box-shadow .18s}
.card:hover{transform:translateY(-6px); box-shadow:0 12px 30px rgba(0,0,0,0.7)}
.card h3{margin:0 0 6px;}
.card p{margin:0;color:var(--muted);font-size:0.96rem}
/* footer */
.footer{margin-top:auto;padding:12px;color:#99b1c9;font-size:13px;border-top:1px solid rgba(255,255,255,0.02)}
/* responsive */
@media (max-width:900px){ .sidebar{display:none} .main{padding:12px} }
